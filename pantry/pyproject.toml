[project]
name = "chef-pantry"
version = "0.1.0"
description = "Shared library for SEC598 Chef — models, clients, and utilities."
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "httpx>=0.28",
]
