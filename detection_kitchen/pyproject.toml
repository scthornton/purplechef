[project]
name = "chef-detection-kitchen"
version = "0.1.0"
description = "Detection Kitchen — test-driven detection engineering pipeline. (Phase 2)"
requires-python = ">=3.12"
dependencies = [
    "chef-pantry",
]

[tool.uv.sources]
chef-pantry = { workspace = true }
