[project]
name = "chef-detection-kitchen"
version = "0.9.0"
description = "Detection Kitchen — test-driven detection engineering pipeline."
requires-python = ">=3.12"
dependencies = [
    "chef-pantry",
    "pyyaml>=6.0",
]

[tool.uv.sources]
chef-pantry = { workspace = true }
