[project]
name = "chef-recipe-book"
version = "0.9.0"
description = "Recipe Book kitchen — purple team recipe orchestration."
requires-python = ">=3.12"
dependencies = [
    "chef-pantry",
    "pyyaml>=6.0",
    "rich>=13.0",
]

[tool.uv.sources]
chef-pantry = { workspace = true }
